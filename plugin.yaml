name: scope-recall
version: 1.0.7
description: Scoped local memory provider with SQLite truth, LanceDB semantic recall, and nightly workflow digest.
hooks:
  - on_turn_start
  - on_session_end
  - on_memory_write
