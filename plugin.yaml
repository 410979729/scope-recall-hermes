name: scope-recall
version: 1.0.9
description: Scoped local memory provider with SQLite truth, optional vector companions, and nightly workflow digest.
hooks:
  - on_turn_start
  - on_session_end
  - on_memory_write
