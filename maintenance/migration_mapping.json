{
  "format": "scope-recall-p15-migration-map/5",
  "old_baseline": "578b955802df753f2e2208e26eab6f71971285a0",
  "new_schema": 1106,
  "principles": {
    "source_and_deletion_first": true,
    "fixed_batch_and_source_keys": true,
    "idempotent": true,
    "candidates_never_auto_promoted": true,
    "vectors_rebuildable_not_semantic_proof": true,
    "migrated_text_credentials_excluded": true,
    "raw_backup_is_protected_snapshot": true
  },
  "fields": [
    {"old": "journal_entries.id", "new": "source_events.event_id", "rule": "deterministic event-legacy-journal sha256 id"},
    {"old": "journal_entries.id", "new": "source_events.source_event_key", "rule": "legacy:journal_entries:<id>"},
    {"old": "journal_entries.content", "new": "source_events.content", "rule": "exact content; no semantic rewrite"},
    {"old": "journal_entries.role", "new": "source_events.role", "rule": "user/assistant/tool/system/document, else unknown"},
    {"old": "journal_entries.created_at", "new": "source_events.occurred_at and recorded_at", "rule": "valid ISO instant preserves instant, otherwise occurred_at null and time_precision unknown"},
    {"old": "journal_entries.scope_id", "new": "source_events.scope_id", "rule": "exact scope; inaccessible scope refuses migration"},
    {"old": "journal_entries.project_id/branch_id", "new": "source_events.project_id/branch_id", "rule": "preserve exact context; missing target binding is a cutover block, never NULL widening"},
    {"old": "journal_entries.shared_scope_id + metadata.scope_mode", "new": "source_events.scope_id + extra_json.scope_authorization", "rule": "scope_id is the already-resolved row scope; local/shared/shared_pool are classified from explicit metadata; non-empty shared_scope_id alone never blocks"},
    {"old": "journal_entries.session_id", "new": "source_events.session_id", "rule": "exact session identity"},
    {"old": "journal_entries.metadata", "new": "source_events.extra_json", "rule": "sanitized legacy metadata plus source table/id; denylisted secret keys omitted"},
    {"old": "memories", "new": "source_events", "rule": "durable memory is imported as source evidence, never as an active claim"},
    {"old": "memory_journal_sources", "new": "evidence_links", "rule": "memory source event derived_from mapped journal source event"},
    {"old": "task_episodes", "new": "episodes + episode_versions + episode_events", "rule": "resume evidence is copied only for mapped sources; missing source remains unknown"},
    {"old": "privacy_purge_operations + privacy_purge_source_tombstones", "new": "deletion_operations + deletion_members + object_blocks + source_events.read_blocked", "rule": "mapped closure includes source, durable summary, episode, fact/evidence archives and entire procedure/archive-version groups when any snapshot depends on a deleted source; Core purge scrubs the closure; rerun never restores scrubbed claims; vector/attachment/history layers remain pending"},
    {"old": "memories.metadata artifact fields", "new": "source_events.extra_json.legacy_artifacts", "rule": "metadata only; no artifact is fabricated or opened"},
    {"old": "fact_claims", "new": "claims + claim_versions + evidence_links", "rule": "single-value current facts with exact live evidence become Core authority; old status/assertion/cardinality/time/IDs remain in payload; no model rewrite"},
    {"old": "fact_claim_evidence", "new": "evidence_links", "rule": "exact source ref and excerpt are linked; missing/non-exact evidence leaves proposed and is reported"},
    {"old": "fact_action_receipts", "new": "sanitized imported source history", "rule": "preserve prior action/idempotency/request/receipt fields as history and associate by old claim ID; new Core never replays old actions"},
    {"old": "procedural_playbooks + playbook_versions", "new": "claims(kind=procedure) + claim_versions + imported archive sources", "rule": "preserve sanitized snapshots and evidence; version archives inherit full parent scope/project/branch at storage write; candidate remains proposed; promoted requires exact same-context evidence; all snapshots participate in source-deletion closure"},
    {"old": "experience_runs, reflection_events, fact_freshness, skill_anchors, skill_conflicts", "new": "sanitized imported source history", "rule": "preserve exact rows and link to procedure/episode where IDs are unambiguous; old governance engines are not runtime dependencies"},
    {"old": "unknown columns on imported content/deletion tables", "new": "structural blocked report before target content writes", "rule": "fixed official 578b column inventory plus project_id/branch_id on scope-owning rows; any other added column blocks all content copying; no ACL guessing or raw row export"},
    {"old": "unknown tables and external files", "new": "unmapped evidence report", "rule": "schema inventory plus sanitized rows; explicit cutover block; no guessing"},
    {"old": "all legacy text/JSON fields", "new": "sanitized target/report text", "rule": "use scope_recall.capture_filters secret/path pipeline; raw backup is a protected snapshot, not an export"}
  ],
  "unmapped": [
    "multi-value fact groups or conflicting single slots that Core claim_slot cannot represent without inventing semantic conditions",
    "current facts without exact live evidence (retained as proposed/archive and reported)",
    "old candidate and experience rows only as executable governance (their history is imported)",
    "old vector/FTS projections",
    "retained attachment bytes without a verifiable source-bound metadata record",
    "legacy deletion target hashes without a source identity",
    "old permissions that cannot be represented by scope_id/project_id/branch_id"
  ]
}
