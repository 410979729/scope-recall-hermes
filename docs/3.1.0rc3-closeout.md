# Scope Recall 3.1.0rc3

基线为 rc2 提交 754f9d95083878c81c1d804348e41a0b3aca7496。协议 1.1、SQLite schema 1108 保持一致。

本候选修复两条实际复现的路径。Codex 原文捕获改走已有持久接收队列，后续派生事务失败不会一并丢失接收记录；排队结果触发有界 worker，并输出不含原文的诊断。候选评估在模型调用前，原子核对当前候选、证据、租约，并记录本次调用的记忆版本；排队期间的无关写入不会让合法结果必然作废。调用期间的版本变化、删除、权限撤销和中断后的重复调用仍受原有检查保护。

定向检查涵盖持久捕获恢复、宿主边界、实际 MCP 子进程和候选生命周期。真实阿甲补齐的配置恢复、调用账本与事实证据见工作区独立交付目录 scope-recall-rc3-root-closeout-20260913。上轮 Sol 的原始失败及条件通过回执保留，不能由后续成功覆盖。

此版本仍是私有验收候选。没有公共发布、推送或版本标签，也不据此承诺完整生产验收已通过。
