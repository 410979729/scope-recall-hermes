# P07 evidence boundaries

The artifact manifest freezes actual source bytes and evidence. Local tests and real model development runs have separate verdicts. No sealed raw/gold content was read by root. No production fleet configuration, public release or desktop input was changed during this development slice.

The development archive contains byte-identical copies of raw source manifests, requests, responses, extraction decisions, new-session output and retained/delivered SVG files. TEST SQLite archives are consistent online backups of the closed development stores, rather than claimed byte-identical filesystem copies. Leases and transient SQLite sidecars are not archived.

Development v1 used schema 1104 and failed extraction 10. Development v2 used schema 1105; requests 11–12 were rejected and 13 accepted. The six source records contain controlled TEST-persona imported human messages with exact provenance and actual SVG write/XML observations. Their occurrence timestamps are fixed fixture values, not claimed wall-clock UI timestamps. No hidden reasoning, production conversation or credential was supplied.

The first reopen attempt raised `TypeError` because the probe treated `(Artifact, bytes)` as bytes. The probe was corrected and the successful independent-process reopen is recorded in `development-v2/new-session.json`. No API call or fabricated successful reopen was counted for the failed local attempt.

Answer 14 failed with unknown usage; the conservative reservation remains in the shared ledger. Its coarse transport error classification does not establish a specific provider failure cause. Answers 15 and 16 are both retained. Answer 16 demonstrates restoration of the required source facts and artifact identity, but still asks for a color direction and does not perform a color edit. It is not a final user-journey pass. The open-item classification omission in accepted extraction 13 also remains visible.

`episodes-1788683179161848700.json` records pytest exit 0, 29 passed and 2 skipped, but the enclosing command failed during temporary-directory cleanup after receipt writing. The original receipt is retained unchanged. The aggregate task receipt overrides this run to overall exit 1 and an independent correction record links the mismatch. Subsequent runner changes defer success until cleanup has completed.

Tests initially failed on Windows path length, extended-path guard normalization and NUL handling. Fixes keep protected-home and out-of-root checks active. The first guarded execution of new runner subprocess tests failed because the guard correctly prohibited child processes (`unit-1788685731247236600.json`, 24 passed / 2 failed); that test harness is repaired without weakening the guard.

The Go runtime uses the documented identifying User-Agent and `x-opencode-session` headers, fresh allowance checks, strict TEST input bounds and one shared cost/token reservation ledger. DeepSeek V4 thinking is explicitly disabled. These request settings were checked against primary documentation: https://opencode.ai/docs/go/ and https://api-docs.deepseek.com/guides/thinking_mode/ . Public provider request parameters are not substituted for actual token usage evidence.

This package is offered for independent G1 review. It does not contain a G1 verdict, final automatic retrieval result, installed-host functional score, or release approval.

J02 supplementary evidence is frozen separately in `failure-resume-artifact-manifest.json`. The v1 probe did not store the actual tool failure in Core and used future fixture occurrence times. The v2 probe did store the tool observation, but its initial human timestamp was taken after the command, and its context environment identifiers were still literal labels. These two preparations had no API calls and are not counted as successful vertical slices. Their earlier author summaries are superseded by actual file inspection. The v2 source is retained in `rejected-j02/probe-v2.py`; the original v1 probe source was not separately frozen.

Root's v3 uses actual ordered timestamps and context fingerprints derived from differing worktree file content. Attempts 17 and 18 returned nonliteral citations of JSON-serialized tool output; both were rejected. The v4 captures actual stdout/stderr text and exit status directly as the tool event, keeping argv, script bytes/hash and wall-clock metadata in the immutable raw manifest. Attempt 19 still failed because it put a statement including unfinished work into verified progress and paraphrased a blocker. No validator was loosened to admit it.

The same v4 raw input and generic consolidation prompt were then sent to the already approved Go DeepSeek V4 Flash (attempt 20) and accepted. This is a recorded extractor choice during development, not a claim that all MiMo extractions succeed. After a separate-process reopen, a different actual file fingerprint produces `needs_revalidation=true`. The current worktree is really checked again: sorting succeeds and export fails because the output directory is absent. Answer 21 has fresh Core episode/source release with an epoch check and a fresh actual tool result; it proposes a supported fix and validation plan. It ends by asking whether to execute, and did not execute that fix. Neither answer 16 nor 21 is counted as a formal complete host journey.

The latest budget snapshot contains 21 attempts and 80,957 micro-USD charged or conservatively reserved against the unchanged 20,000,000 micro-USD cap. Actual model evidence is retained for every attempt; no best-of-only success denominator is reported.

Independent authority probes exposed unpunctuated questions being treated as assertions, including a capture fast-path revision and a forget request. A shared source-qualification guard now covers claims, capture revisions, deletion, episode states/resume fields and reference clarification. Negative cancellation markers must preserve their local qualifiers. Actual Core regressions include both rejected mutations with unchanged authority and valid positive assertions; the earlier failing runs remain in this archive.

An episode resume cannot justify artifact version 2 merely by citing a source that mentions the stable artifact ID or observes only version 1. The exact version must occur in a cited observed display snapshot, or the resume must cite the source used to register that exact version. The regression exercises both rejection and a supported version-2 continuation through the real transaction boundary.

During the deletion test addition, two trailing assertions belonging to the existing purge-retry test were accidentally placed in the new question test. They were restored to the original test before the successful 28-test deletion run. This was a test-editing defect, not evidence of a production purge failure.

The M11 structural gap is addressed using an existing sourced naming Claim as the stable target and a separately sourced alias Claim as the rename relation. Tests first capture raw text and submit bounded proposals through actual Core validation. The target must be an effective naming fact in the exact project/branch/scope, and a human quote must explicitly connect its subject or old name to the complete new name. Existing color facts, unrelated name mentions, other projects, name prefixes, questions and hypothetical renames do not establish aliases. The original name remains stored. These deterministic proposal tests do not claim model extraction or automatic alias retrieval success; P08 owns retrieval.

The first frozen candidate `ca055f73c930a1bf7c13a8d442d35bc9d5eced60` received an independent G1 `NO_GO`, retained in `verification/G1/audit-ca055f73.json`. Its 338 passing deterministic checks and one unavailable-symlink skip did not cover a name-suffix and clipped-quote authority defect. Three independent actual Core probes accepted an unsupported current alias. The following repair checks the naming relation and local polarity in the complete captured human source, then requires the quoted span to support that same relation. Passing the earlier suites never supersedes this recorded failure or the need for independent re-adjudication.
