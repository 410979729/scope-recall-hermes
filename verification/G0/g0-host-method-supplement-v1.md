# G0 宿主测试方法补充 v1

状态：**PREDECLARED_PENDING_EQUIVALENCE**。本记录预先声明方法修订及独立审计条件，不签署等价通过、G0/P12/P18 通过或正式评测放行。

## 授权与冻结关系

根任务转交的用户最新授权是全程自主决策、无需再次批准，并明确要求大量并行；本补充在隔离 TEST、本地候选范围内执行该授权。规格 `START_HERE.md:34` 原文：“用户是否授权阶段内自动推进，以实际启动指令为准。”并行不扩大外发、计数、隔离或工具信任权限。

原 W 协议不改写：`F:/SCOPERECALL更新项目/worktrees/scope-recall-v1.1/verification/P18/p18-evaluation-protocol-v1.json`，本次读取 SHA256 为 `45b70192857b5b8645dd2bf409db55f320f1cf31f297e89910f9f340e76d32d4`。R 读取时 HEAD 为 `dda646d50f5c416812c029c209c916bf007459a6`；这不是后续最终候选冻结的替代。

原方法是 Hermes 0.21.0 真实 A2A 与 Codex Windows desktop 实际 UI 输入/发送；Codex CLI 替代被明文禁止。前者不能承担操作员授权：冻结 Hermes A2A `security.py:134–164` 将远端材料视为不可信外部输入并阻断操作员 slash 命令。后者的鼠标/窗口控制已被 Escape 中止（根任务报告）；不能把物理控制继续可用作为前提，也不以后台 CLI 冒称原 UI 方法完成。

## 仅预声明两项方法修订

1. **Hermes 同一官方 0.21.0 增加 CLI 操作员入口。** CLI 承担当前明确获准的现场纠正/删除，A2A 承担非权威输入捕获、新会话查询及越权拒绝验证。CLI 使用现有可信操作员上下文；不能仅凭 `role=user` 将合成转述认作真人原话。A2A 默认 `origin_unknown` 的产品收紧保留，不新增 origin、身份后门或 A2A 操作员权限。
2. **Codex 官方 `mcp__codex_app.send_message_to_thread` 仅进入等价诊断候选。** 在现有隔离桌面任务后台提交提示，不做 CUA 点击，不控制鼠标/窗口，不绕过插件信任，不用 CLI 替代。只有下列实证齐全才可把它冻结为 desktop app-server 测试入口。现有资料不能断言此工具在本机版本实际触发哪些 Hooks。根任务报告已安排最多两次诊断，结果未到；本记录不声称它们已执行或成功。

## 正式启用前必须提交的等价证据

- **Hermes：** 固定官方源码/宿主版本、候选包和模型；逐臂证明 CLI 与 A2A 绑定同一隔离 TEST 实例、installation/principal、project/scope 和权威库，而非导出成品库接力；保留 CLI 现场命令及当前授权来源、变更回执/版本/删除栅栏，并在 A2A 清洁新会话验证结果。另以负例证明 A2A 来源仍非人类、不能执行同类操作员命令，且没有来源提权。CLI/A2A 分别归因，不能把组合结果称作 A2A 操作员能力。
- **Codex：** 证明官方任务工具指向当前实际 desktop 进程及目标 TEST 任务，记录 host/package、task/session/turn 关联、目录、安装身份、scope、权威库、模型/工具配置与已存在的插件信任；逐次留存真实 `UserPromptSubmit`、`Stop` 和适用的 Session 生命周期回调。对应提示、捕获源、只读召回、`additionalContext` 实际进入目标回合且只交付一次、最终可见输出必须可关联。已有 UI 收据用于行为对照，工具“已发送”回执不能证明等价。涉及附件的冻结行为仍须证明版本与可访问路径；没有回调或实际交付证据即保留阻塞。
- **共同：** 在正式运行前冻结各旅程使用哪个入口及步骤映射，四臂使用相同安排、原始历史、模型、工具、上下文和记忆预算。每臂/每宿主独立 TEST home/数据库，禁止跨臂或跨宿主移交数据库、回忆包或运行答案。来源/对象/召回/实际注入/最终行为分别留证，入口分账不缩减端到端分母。

## 完全不变的验收条件

120 个独立核心留出（240 配对变体不重复计独立样本）；每宿主 40 查询及每条 8 轮的 8 条完整旅程；四臂 A 原生记忆/B 固定 578b 旧版/C 最终新版/D 同档案简单搜索；两宿主、既定模型与路由；L3 整体必要证据覆盖至少 90%、各 B 组至少 80%，L4 至少 36/40 且 8/8 旅程完整；确定性来源、当前/历史、授权、删除、隔离断言全部正确，安全失败阻断。

API 折算总帽仍为 **$20**，原持久账本、请求/token/提交轮数帽、调用前最大预留和未知用量保留不变；诊断、并行、辅助调用、失败和复跑均计入原账本。原始失败与旧收据全部保留，不选择最好结果、不减步骤、不改 gold、不预写正确 claims/答案、不因评测失败调整门槛。真实自动提取、候选/runner 冻结及其他正式执行前置条件不被本补充豁免。

## 独立审计裁决与边界

规格 `docs/05_验收测试与独立审计.md:146` 要求 Hermes、Codex 各有一个用户实际使用并端到端验证的入口；`docs/07_人类式记忆功能定稿与验收.md:387–409` 要求四层及真实新会话行为证据。上述变更可以作为保持产品目标与安全权限的测试方法修正，**前提是证据证明等价，且未删减冻结行为**。若只把无法完成的现场行为换成离线预置状态或少测入口，裁决为不等价，不能签 PASS。

本审计线仅读取公开规格、公开实现及协议元数据，并新增本 Markdown 和同名 JSON；未读 sealed raw/gold，未改产品、测试或 fixture，未启动宿主、模型、UI 或执行正式评测。
